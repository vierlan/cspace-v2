<div>
  <h1 class="font-bold text-4xl">Edit my package</h1>
  <p>Find me in app/views/packages/edit.html.erb</p>
</div>

<div>
  <%= link_to "back to my Venues", my_venues_path(current_user) %>
</div>
<div class="grid grid-cols-8 p-10 m-4 mx-auto">
  <div class=""></div>
  <div class="col-span-8 sm:col-span-8 md:col-span-6 lg:col-span-4 bg-white rounded shadow p-4">
    <%= simple_form_for([@venue, @package]) do |f| %>
      <%= f.input :package_name %>
      <%= f.input :package_description, placeholder: 'Freshly baked in house' %>
      <%= f.input :package_duration, :value => 'hours', hint:'time in hours the package includes' %>
      <%= f.input :photo, as: :file, input_html: {multiple: false} %>
      <div class="input-group">
        <%= f.input :package_price, :value => '€', tabindex: 25 %>
        <%= f.submit "Save changes to package", class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
